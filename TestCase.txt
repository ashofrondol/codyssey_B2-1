# ============================================================
# budget_app CLI 종료 코드 계약 테스트 케이스
#
# 형식:  <서브커맨드와 옵션...>  =>  <기대 종료코드>
#        !0 은 "0이 아닌 값"(오류로 끝나야 함)을 뜻한다.
#
# - '#' 로 시작하는 줄과 빈 줄은 무시된다.
# - 각 케이스는 비어 있는 임시 데이터 폴더에서 독립 실행된다.
# - tests/test_from_testcases.py 가 이 파일을 읽어 자동으로 케이스를 만든다.
#   (dev-env-bootstrap 템플릿의 `함수인자 => 기대값` 규약을 CLI 용으로 확장)
# ============================================================

# ---------- 정상 종료(0) ----------
--help                                  => 0
list                                    => 0
category list                           => 0
summary --month 2024-01                 => 0

# ---------- 잘못된 입력 → 0이 아닌 종료 코드 (체크리스트 1.7) ----------
nosuchcommand                           => !0
summary --month 2024-13                 => !0
summary --month notamonth               => !0
delete --id NOPE-999999                 => !0
update --id NOPE-999999 --amount 100    => !0
category remove --name no_such_category => !0
budget set --month 2024-99 --amount 100 => !0

# ---------- 파일 오류 → 0이 아닌 종료 코드 (체크리스트 1.6) ----------
import --from __no_such_file__.csv      => !0
